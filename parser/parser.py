def parser(tokens):
    print("Parser called")
    print(tokens)
    
    # Parse the tokens and create an Abstract Syntax Tree (AST)
    # Handle different types of expressions (assignment, computation, function definition)
    # Return the AST